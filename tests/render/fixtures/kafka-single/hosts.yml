---
# One broker. The role documents that a single-member group renders exactly as
# it did before clustering was added, and this fixture is what holds it to that
# — the claim is otherwise only in a comment.
all:
  children:
    message_broker:
      hosts:
        kafka-01:
    render_target:
      hosts:
        kafka-01:
