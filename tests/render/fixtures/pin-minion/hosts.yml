---
# The role reads no inventory groups for these files. What matters is that the
# package list is this role's own.
all:
  children:
    minion:
      hosts:
        minion-01:
    render_target:
      hosts:
        minion-01:
