---
# One member: must render the single-node form — memberlist carrying only the
# advertise address, in-memory rings, replication factor 1, filesystem storage.
all:
  children:
    mimir:
      hosts:
        mimir-01:
    render_target:
      hosts:
        mimir-01:
