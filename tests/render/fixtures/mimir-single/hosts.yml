---
# One member: must render the pre-clustering single-node form — no memberlist,
# in-memory rings, replication factor 1, filesystem storage.
all:
  children:
    mimir:
      hosts:
        mimir-01:
    render_target:
      hosts:
        mimir-01:
