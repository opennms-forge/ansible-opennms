---
# A Minion with profiling switched on. The role reads no inventory groups for
# this file, so the shape here is minimal — what matters is the values.
all:
  children:
    minion:
      hosts:
        minion-01:
    render_target:
      hosts:
        minion-01:
