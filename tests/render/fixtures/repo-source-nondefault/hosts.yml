---
# The role reads no inventory groups for this file; what matters is that
# `opennms_version` comes from the caller rather than the role's fallback.
all:
  children:
    core:
      hosts:
        core-01:
    render_target:
      hosts:
        core-01:
