---
- name: Verify credentials before anything else
  ansible.builtin.include_tasks: 00-credentials-check.yml

- name: Verify the host clock is synchronised
  ansible.builtin.include_role:
    name: timesync

- name: Include OpenNMS OpenJDK roles
  ansible.builtin.include_role:
    name: openjdk
  vars:
    openjdk_version: 21
    openjdk_pkg_version: "21*"

- name: Include OpenNMS repository roles
  ansible.builtin.include_role:
    name: opennms_repositories
  vars:
    # This role's own OpenNMS release decides which repository major to use.
    # Passed at include time rather than left to defaults precedence between
    # two roles, which depends on load order.
    opennms_repo_major: "{{ opennms_version.split('.')[0] }}"

# Written before the packages are installed, so a deliberate `opennms_version`
# bump rewrites the pin and permits the new version in the same run. Reversing
# these two would mean the old pin blocks the upgrade the run is trying to make.
- name: Pin OpenNMS packages to {{ opennms_version }}
  ansible.builtin.template:
    src: etc/apt-preferences-opennms.j2
    dest: "{{ opennms_apt_preferences_file }}"
    owner: root
    group: root
    mode: "0644"
  tags:
    - opennms
    - packages

- name: Installing OpenNMS Sentinel
  ansible.builtin.apt:
    name: "opennms-sentinel={{ opennms_pkg_version }}"
    state: present
    install_recommends: false
  tags:
    - opennms
    - sentinel
    - package

- name: Enable Kafka as and flow persistence
  ansible.builtin.copy:
    src: featuresBoot.d/flows.boot
    dest: "{{ opennms_sentinel_home }}/etc/featuresBoot.d"
    owner: sentinel
    group: sentinel
    mode: '0640'
  when: opennms_elastic_flows | length > 0
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - kafka
    - flows

- name: Configure the distributed data sources
  ansible.builtin.template:
    src: org.opennms.netmgt.distributed.datasource.cfg.j2
    dest: "{{ opennms_sentinel_home }}/etc/org.opennms.netmgt.distributed.datasource.cfg"
    owner: sentinel
    group: sentinel
    mode: "0640"
  with_dict: "{{ opennms_sentinel_distributed_datasource }}"
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - configuration

- name: Configure the Elasticsearch flow persistence
  ansible.builtin.template:
    src: org.opennms.features.flows.persistence.elastic.cfg.j2
    dest: "{{ opennms_sentinel_home }}/etc/org.opennms.features.flows.persistence.elastic.cfg"
    owner: sentinel
    group: sentinel
    mode: "0640"
  when: opennms_elastic_flows | length > 0
  no_log: true
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - configuration

- name: Remove Elasticsearch flow persistence config when not handled by Sentinel
  ansible.builtin.file:
    path: "{{ opennms_sentinel_home }}/etc/org.opennms.features.flows.persistence.elastic.cfg"
    state: absent
  when: opennms_elastic_flows | length == 0
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - configuration
    - elasticsearch

- name: Configure the Sentinel controller
  ansible.builtin.template:
    src: org.opennms.sentinel.controller.cfg.j2
    dest: "{{ opennms_sentinel_home }}/etc/org.opennms.sentinel.controller.cfg"
    owner: sentinel
    group: sentinel
    mode: "0640"
  with_dict: "{{ opennms_sentinel_controller }}"
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - configuration

- name: Configure the Sentinel consumer for Kafka message broker
  ansible.builtin.template:
    src: org.opennms.core.ipc.sink.kafka.consumer.cfg.j2
    dest: "{{ opennms_sentinel_home }}/etc/org.opennms.core.ipc.sink.kafka.consumer.cfg"
    owner: sentinel
    group: sentinel
    mode: "0640"
  with_dict: "{{ opennms_sentinel_kafka }}"
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - kafka

- name: Configure the Sentinel sink for Kafka message broker
  ansible.builtin.template:
    src: org.opennms.core.ipc.sink.kafka.cfg.j2
    dest: "{{ opennms_sentinel_home }}/etc/org.opennms.core.ipc.sink.kafka.cfg"
    owner: sentinel
    group: sentinel
    mode: "0640"
  with_dict: "{{ opennms_sentinel_kafka }}"
  notify:
    - Restart sentinel
  tags:
    - opennms
    - sentinel
    - kafka

- name: Enable systemd unit for OpenNMS Sentinel
  ansible.builtin.service:
    name: sentinel.service
    enabled: true
  tags:
    - opennms
    - sentinel
    - systemd

- name: Start systemd unit for OpenNMS Sentinel
  ansible.builtin.service:
    name: sentinel.service
    state: started
  when:
    - skip_sentinel_startup | default("false") == "false"
  tags:
    - opennms
    - sentinel
    - systemd
