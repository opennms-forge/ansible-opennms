#########
# DO NOT EDIT THIS FILE
# Managed by Ansible
######
# Karaf PID: {{ item.pid }}   (rendered from `{{ item.var }}`)
#
# This file is a COMPLETE configuration, not an override of
# org.opennms.core.ipc.kafka.cfg. OpenNMS uses whichever file wins in full:
# OsgiKafkaConfigProvider takes this PID's properties if they contain
# `bootstrap.servers`, and otherwise ignores this file entirely and falls back
# to the common PID.
{% for (key, value) in item.properties.items() %}
{{ key }} = {{ value }}
{% endfor %}
