<!--
Thanks for the contribution. CONTRIBUTING.md has the details; the short version:
commits need `git commit -s`, and AI-assisted ones need an `Assisted-by:` trailer.
-->

Closes #

## What changes

<!-- What does this do, and why? If it fixes a bug, what was the behaviour before? -->

## How it was verified

<!--
Evidence, not assertions. What did you run, against what, and what came back?
"Deployed stub_pgsql against a fresh Ubuntu 24.04 host; apt-get update fetched
the pgdg repo with no GPG error" beats "tested and works".

If you could not test something, say which part and why — that is far more
useful than silence.
-->

- [ ] `make lint` passes
- [ ] `make check-urls` passes (if any download URL or version variable changed)
- [ ] Verified against a real target host

## Notes for the reviewer

<!--
Anything that would not be obvious from the diff: a rejected alternative, a
known limitation, something deliberately left out of scope. Delete if empty.
-->
